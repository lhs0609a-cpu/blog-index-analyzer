# ================================================
# Blog Index Analyzer Backend - Environment Variables
# ================================================
# 이 파일을 복사하여 .env 파일로 저장하고 값을 입력하세요
# cp .env.example .env

# ================================================
# 데이터베이스 설정 (Supabase)
# ================================================
# Supabase 프로젝트 URL (대시보드 → Settings → API)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Service Role Key (백엔드 전용)
# 대시보드 → Settings → API → service_role key
# ⚠️ 절대 프론트엔드에 노출하지 마세요!
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# PostgreSQL 직접 연결 URL (선택)
# 대시보드 → Settings → Database → Connection string → URI
DATABASE_URL=postgresql://postgres:PASSWORD@db.xxxxx.supabase.co:5432/postgres

# ================================================
# 인증 설정 (JWT)
# ================================================
# JWT 비밀 키 (최소 32자 이상의 랜덤 문자열)
# 생성: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-very-secret-key-here-minimum-32-characters

# JWT 알고리즘 (변경 불필요)
JWT_ALGORITHM=HS256

# 토큰 만료 시간 (분) - 기본 7일
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ================================================
# 서버 설정
# ================================================
# 환경 (development, staging, production)
ENVIRONMENT=production

# 디버그 모드 (production에서는 false)
DEBUG=false

# API 호스트/포트
HOST=0.0.0.0
PORT=8000

# ================================================
# 외부 API 설정 (선택)
# ================================================
# Anthropic API (AI 기능 사용 시)
# ANTHROPIC_API_KEY=sk-ant-...

# TossPayments (결제 기능 사용 시)
# TOSS_CLIENT_KEY=test_ck_...
# TOSS_SECRET_KEY=test_sk_...

# ================================================
# 기타 설정
# ================================================
# 로깅 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# CORS 허용 도메인 (쉼표로 구분)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
